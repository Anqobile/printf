#include <stdio.h>

/**
  *main - start point
  *Return: Always 0 
  */

int main() 
{
    int number = -42;

    printf("%d\n", number); // Signed decimal integer
    printf("%i\n", number); // Signed decimal integer (alternative specifier)

    return 0;
}
